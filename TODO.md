# TODO

## Evaluation And Test Closure

- Expand focused unit tests for RAG ingest, audit fallback, and external dependency failure modes.
- Grow `evals/datasets/*.jsonl` beyond the current smoke suites for routing, tool use, RAG retrieval, grounded answer, and safety.
- Add helper scripts for MySQL checks, remote AI checks, table initialization validation, and Langfuse dataset sync.
- Add `promptfoo.yaml` after the first eval datasets are stable.
