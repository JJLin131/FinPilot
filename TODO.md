# TODO

## Evaluation And Test Closure

- Add `tests/` with focused unit tests for routing, RAG ingest, BM25 retrieval, audit fallback, and memory filtering.
- Add `evals/datasets/*.jsonl` suites for routing, tool use, RAG retrieval, grounded answer, and safety.
- Add helper scripts for MySQL checks, remote AI checks, table initialization validation, and Langfuse dataset sync.
- Add `promptfoo.yaml` after the first eval datasets are stable.
